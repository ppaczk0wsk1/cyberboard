services:
  cyberboard:
    image: ghcr.io/ppaczk0wsk1/cyberboard:latest
    container_name: cyberboard
    ports:
      - "8900:8900"
    volumes:
      - ./data.json:/app/data.json
    restart: unless-stopped
